[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Lets the frontend keep calling fetch("/api/chat") unchanged —
# Netlify quietly routes it to the actual function path.
[[redirects]]
  from = "/api/chat"
  to = "/.netlify/functions/chat"
  status = 200
